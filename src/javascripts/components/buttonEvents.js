// import eat from './eat/eat';

const buttonEvents = () => {
  // document.getElementById('healthy').addEventListener('click', eat.full);
  // document.getElementById('unhealthy').addEventListener('click', eat.full);
};

export default { buttonEvents };
